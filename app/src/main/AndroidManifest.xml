<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zhuandian.travel">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".business.activity.HelperSelectLocalActivity"></activity>
        <activity android:name=".business.activity.SendHelpActivity" />
        <activity android:name=".business.activity.FindHelperActivity" />
        <activity android:name=".business.activity.SelectFriendActivity" />
        <activity android:name=".business.activity.GuideSearchResultActivity" />
        <activity android:name=".business.activity.SearchGuideActivity" />
        <activity android:name=".business.activity.TravelDiaryDetailActivity" />
        <activity android:name=".business.activity.NewTravelDiaryActivity" />
        <activity android:name=".business.activity.TravelGuideDetailActivity" />
        <activity android:name=".business.activity.NewTravelGuideActivity" />
        <activity android:name=".business.activity.FoodsAndViewsDetailActivity" />
        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="adjustPan">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity android:name=".business.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".business.login.UserRegisterActivity" />

        <provider
            android:name="cn.bmob.v3.util.BmobContentProvider"
            android:authorities="com.zhuandian.travel.BmobContentProvider" />
    </application>

</manifest>